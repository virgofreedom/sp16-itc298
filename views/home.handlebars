
    
    <div>
        
    </div>
    <form action="/search" method="POST">
        <input type="text" name="title" placeholder="Games Title" required/>
        <input type="submit" name="submit" value="Search"/>   
    </form>
        
    <br>
    <fieldset class="small-6 columns">
    <a href="/add"><button>Add New Game</button></a>
        <legend>Game's list</legend>
    <table class="table table-striped table-bordered">
        <tr><th>Game's title</th></tr>
        {{#each game_title}}
        <tr>
            <td><a href="?gtitle={{.}}">{{.}}</a></td>
        {{/each}}
        
        </tr>
    </table>    
    
    </fieldset>
    <fieldset class="small-6 columns">
        <legend>Detail page</legend>
        <div class="page-header">
            <h1>{{res_title}}</h1>
        </div>
        
        <p>{{res_plateforme}}<br>{{res_price}}</p>
    </fieldset>
